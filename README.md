# Vim/Neovim 配置文档

## 目录

1. [简介](#简介)
2. [安装和设置](#安装和设置)
3. [基础配置说明](#基础配置说明)
4. [Vim 基本操作](#vim-基本操作)
5. [高级操作技巧](#高级操作技巧)
6. [自定义键位映射](#自定义键位映射)
7. [插件推荐](#插件推荐)
8. [故障排除](#故障排除)

---

## 简介

这是一个精心配置的 Vim/Neovim 配置文件，旨在提供高效的编辑体验。配置文件包含了常用的设置和键位映射，让你能够更流畅地使用 Vim。

### 主要特性

- ✅ 语法高亮
- ✅ 相对行号显示
- ✅ 智能搜索和高亮
- ✅ 自动缩进和格式化
- ✅ 系统剪贴板集成
- ✅ 撤销历史保存
- ✅ 自定义状态栏
- ✅ 窗口快速导航
- ✅ Java 文件特定支持

---

## 安装和设置

### 1. 安装 Vim 或 Neovim

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install vim
# 或安装 Neovim
sudo apt install neovim
```

**macOS:**
```bash
brew install vim
# 或安装 Neovim
brew install neovim
```

### 2. 应用配置文件

**方法一：直接复制**
```bash
cp .vimrc ~/.vimrc
```

**方法二：创建符号链接**
```bash
ln -s $(pwd)/.vimrc ~/.vimrc
```

**对于 Neovim 用户：**
```bash
mkdir -p ~/.config/nvim
ln -s ~/.vimrc ~/.config/nvim/init.vim
```

### 3. 重新加载配置

打开 Vim 后执行：
```vim
:source ~/.vimrc
```

或者重新打开 Vim 即可。

---

## 基础配置说明

### 显示设置

| 配置项 | 说明 |
|--------|------|
| `set number` | 显示绝对行号 |
| `set relativenumber` | 显示相对行号（便于快速跳转） |
| `set cursorline` | 高亮当前行 |
| `syntax on` | 启用语法高亮 |
| `set showcmd` | 显示输入的命令 |
| `set showmode` | 显示当前模式（INSERT、VISUAL 等） |

### 编辑设置

| 配置项 | 说明 |
|--------|------|
| `set autoindent` | 自动缩进 |
| `set smartindent` | 智能缩进 |
| `set expandtab` | 用空格替代 Tab |
| `set tabstop=4` | Tab 宽度为 4 个空格 |
| `set shiftwidth=4` | 缩进宽度为 4 个空格 |

### 搜索设置

| 配置项 | 说明 |
|--------|------|
| `set ignorecase` | 搜索时忽略大小写 |
| `set smartcase` | 如果搜索包含大写字母则区分大小写 |
| `set hlsearch` | 高亮搜索结果 |
| `set incsearch` | 实时搜索（边输入边匹配） |

### 其他重要设置

| 配置项 | 说明 |
|--------|------|
| `set mouse=a` | 启用鼠标支持 |
| `set clipboard=unnamed` | 使用系统剪贴板 |
| `set undofile` | 保存撤销历史到文件 |
| `set autoread` | 自动读取外部修改 |

---

## Vim 基本操作

### 模式切换

Vim 有多种操作模式：

| 模式 | 说明 | 进入方式 | 退出方式 |
|------|------|----------|----------|
| **普通模式** (Normal) | 默认模式，用于移动和执行命令 | 启动 Vim 时 | - |
| **插入模式** (Insert) | 编辑文本 | `i`, `a`, `o` 等 | `Esc` |
| **可视模式** (Visual) | 选择文本 | `v`, `V`, `Ctrl+v` | `Esc` |
| **命令模式** (Command) | 执行命令 | `:` | `Enter` 或 `Esc` |

### 基本移动

#### 字符级移动

```
h - 左移一个字符
j - 下移一行
k - 上移一行
l - 右移一个字符
```

#### 单词级移动

```
w  - 移动到下一个单词的开头
b  - 移动到上一个单词的开头
e  - 移动到当前/下一个单词的结尾
ge - 移动到上一个单词的结尾
```

#### 行内移动

```
0  - 移动到行首
^  - 移动到行首第一个非空字符
$  - 移动到行尾
g_ - 移动到行尾最后一个非空字符
```

#### 屏幕移动

```
H  - 移动到屏幕顶部
M  - 移动到屏幕中间
L  - 移动到屏幕底部
gg - 移动到文件开头
G  - 移动到文件结尾
:n 或 nG - 移动到第 n 行
```

#### 翻页

```
Ctrl+f - 向下翻一页
Ctrl+b - 向上翻一页
Ctrl+d - 向下翻半页
Ctrl+u - 向上翻半页
```

### 编辑操作

#### 插入文本

```
i  - 在光标前插入
a  - 在光标后插入
I  - 在行首插入
A  - 在行尾插入
o  - 在下方插入新行
O  - 在上方插入新行
```

#### 删除操作

```
x   - 删除当前字符
X   - 删除前一个字符
dw  - 删除到下一个单词开头
de  - 删除到单词结尾
db  - 删除到上一个单词开头
dd  - 删除整行
D   - 删除到行尾
d0  - 删除到行首
d$  - 删除到行尾（同 D）
3dd - 删除 3 行
```

#### 复制和粘贴

```
yy  - 复制当前行
yw  - 复制一个单词
y$  - 复制到行尾
p   - 在光标后粘贴
P   - 在光标前粘贴
3yy - 复制 3 行
```

#### 撤销和重做

```
u       - 撤销
Ctrl+r  - 重做
```

#### 查找和替换

```
/pattern   - 向下搜索 pattern
?pattern   - 向上搜索 pattern
n          - 跳到下一个匹配
N          - 跳到上一个匹配

:s/old/new/     - 替换当前行第一个匹配
:s/old/new/g    - 替换当前行所有匹配
:%s/old/new/g   - 替换整个文件所有匹配
:%s/old/new/gc  - 替换整个文件所有匹配（需要确认）
```

### 文件操作

```
:w          - 保存文件
:w filename - 另存为
:q          - 退出
:q!         - 强制退出（不保存）
:wq 或 :x   - 保存并退出
ZZ          - 保存并退出（普通模式）
:e filename - 打开文件
:bn         - 切换到下一个缓冲区
:bp         - 切换到上一个缓冲区
```

---

## 高级操作技巧

### 可视模式操作

```
v       - 字符可视模式
V       - 行可视模式
Ctrl+v  - 块可视模式
```

**可视模式下的操作：**
```
d  - 删除选中内容
y  - 复制选中内容
c  - 删除并进入插入模式
>  - 向右缩进
<  - 向左缩进
=  - 自动缩进
```

### 多行编辑（块模式）

1. `Ctrl+v` 进入块可视模式
2. 使用 `j/k` 选择多行
3. `I` 进入插入模式，输入内容
4. `Esc` 应用到所有选中的行

### 宏录制

```
qa      - 开始录制宏到寄存器 a
<操作>  - 执行你要录制的操作
q       - 停止录制
@a      - 执行宏 a
@@      - 重复上次执行的宏
5@a     - 执行宏 a 五次
```

### 标记和跳转

```
ma  - 在当前位置创建标记 a
'a  - 跳转到标记 a 所在行的开头
`a  - 跳转到标记 a 的精确位置
''  - 跳转到上次跳转前的位置
```

### 窗口操作

```
:split 或 :sp   - 水平分割窗口
:vsplit 或 :vs  - 垂直分割窗口
Ctrl+w h        - 移动到左边窗口
Ctrl+w j        - 移动到下边窗口
Ctrl+w k        - 移动到上边窗口
Ctrl+w l        - 移动到右边窗口
Ctrl+w w        - 循环切换窗口
Ctrl+w q        - 关闭当前窗口
Ctrl+w =        - 平均分配窗口大小
```

**注意：** 本配置已将窗口导航映射简化，你可以直接使用：
- `Ctrl+h` - 左
- `Ctrl+j` - 下
- `Ctrl+k` - 上
- `Ctrl+l` - 右

### Tab 页面操作

```
:tabnew       - 新建标签页
:tabnew file  - 在新标签页中打开文件
gt            - 切换到下一个标签页
gT            - 切换到上一个标签页
:tabclose     - 关闭当前标签页
:tabonly      - 关闭其他所有标签页
```

---

## 自定义键位映射

本配置使用 **空格键** 作为 Leader 键，以下是自定义的快捷键：

### 文件操作

| 快捷键 | 功能 | 模式 |
|--------|------|------|
| `<Space>w` | 快速保存文件 | 普通模式 |
| `<Space>q` | 快速退出 | 普通模式 |
| `<Space>h` | 取消搜索高亮 | 普通模式 |

### 剪贴板操作

| 快捷键 | 功能 | 模式 |
|--------|------|------|
| `<Space>y` | 复制到系统剪贴板 | 普通/可视模式 |
| `<Space>p` | 从系统剪贴板粘贴 | 普通/可视模式 |

### 可视模式增强

| 快捷键 | 功能 | 模式 |
|--------|------|------|
| `J` | 向下移动选中的行 | 可视模式 |
| `K` | 向上移动选中的行 | 可视模式 |
| `<` | 向左缩进（保持选择） | 可视模式 |
| `>` | 向右缩进（保持选择） | 可视模式 |

### 窗口导航

| 快捷键 | 功能 | 模式 |
|--------|------|------|
| `Ctrl+h` | 移动到左边窗口 | 普通模式 |
| `Ctrl+j` | 移动到下边窗口 | 普通模式 |
| `Ctrl+k` | 移动到上边窗口 | 普通模式 |
| `Ctrl+l` | 移动到右边窗口 | 普通模式 |

---

## 插件推荐

虽然当前配置是一个纯 Vim 配置（无插件），但以下是一些强烈推荐的插件，可以进一步提升你的 Vim 体验：

### 插件管理器

#### vim-plug (推荐)

**安装：**
```bash
curl -fLo ~/.vim/autoload/plug.vim --create-dirs \
    https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim
```

**使用方法：**

在 `.vimrc` 中添加：
```vim
call plug#begin('~/.vim/plugged')

" 在这里添加你的插件

call plug#end()
```

然后在 Vim 中执行 `:PlugInstall`

### 必备插件列表

#### 1. 文件浏览和导航

**NERDTree** - 文件树浏览器
```vim
Plug 'preservim/nerdtree'
```

快捷键配置：
```vim
nnoremap <leader>n :NERDTreeToggle<CR>
```

**fzf.vim** - 模糊查找文件
```vim
Plug 'junegunn/fzf', { 'do': { -> fzf#install() } }
Plug 'junegunn/fzf.vim'
```

快捷键：
- `:Files` - 查找文件
- `:Buffers` - 查找缓冲区
- `:Rg` - 搜索内容（需要安装 ripgrep）

#### 2. 代码补全

**coc.nvim** - 智能代码补全（推荐）
```vim
Plug 'neoclide/coc.nvim', {'branch': 'release'}
```

支持：
- LSP（语言服务器协议）
- 自动补全
- 代码诊断
- 代码格式化
- 跳转定义

**Java 开发配置：**
```vim
:CocInstall coc-java
```

#### 3. 语法和外观

**vim-polyglot** - 多语言语法高亮
```vim
Plug 'sheerun/vim-polyglot'
```

**gruvbox** - 配色方案（推荐）
```vim
Plug 'morhetz/gruvbox'
```

使用：
```vim
colorscheme gruvbox
```

**vim-airline** - 美化状态栏
```vim
Plug 'vim-airline/vim-airline'
Plug 'vim-airline/vim-airline-themes'
```

#### 4. Git 集成

**vim-fugitive** - Git 命令集成
```vim
Plug 'tpope/vim-fugitive'
```

常用命令：
- `:Git` - 执行 Git 命令
- `:Git blame` - 查看 blame
- `:Git diff` - 查看差异

**vim-gitgutter** - 显示 Git 变更
```vim
Plug 'airblade/vim-gitgutter'
```

在行号旁显示：
- `+` 新增行
- `-` 删除行
- `~` 修改行

#### 5. 编程增强

**vim-commentary** - 快速注释
```vim
Plug 'tpope/vim-commentary'
```

使用：
- `gcc` - 注释/取消注释当前行
- `gc` + motion - 注释多行
- 可视模式下 `gc` - 注释选中内容

**auto-pairs** - 自动补全括号
```vim
Plug 'jiangmiao/auto-pairs'
```

**vim-surround** - 快速修改包围符号
```vim
Plug 'tpope/vim-surround'
```

示例：
- `cs"'` - 将双引号改为单引号
- `ds"` - 删除双引号
- `ysiw]` - 在单词周围加方括号

#### 6. 代码格式化

**vim-prettier** - 代码格式化（适用于 Web 开发）
```vim
Plug 'prettier/vim-prettier', { 'do': 'yarn install' }
```

**ALE** - 异步语法检查
```vim
Plug 'dense-analysis/ale'
```

### 完整插件配置示例

在 `.vimrc` 中添加：

```vim
" ============================================================================
" 插件管理（使用 vim-plug）
" ============================================================================

call plug#begin('~/.vim/plugged')

" 文件浏览
Plug 'preservim/nerdtree'
Plug 'junegunn/fzf', { 'do': { -> fzf#install() } }
Plug 'junegunn/fzf.vim'

" 代码补全
Plug 'neoclide/coc.nvim', {'branch': 'release'}

" 语法和外观
Plug 'sheerun/vim-polyglot'
Plug 'morhetz/gruvbox'
Plug 'vim-airline/vim-airline'
Plug 'vim-airline/vim-airline-themes'

" Git 集成
Plug 'tpope/vim-fugitive'
Plug 'airblade/vim-gitgutter'

" 编程增强
Plug 'tpope/vim-commentary'
Plug 'jiangmiao/auto-pairs'
Plug 'tpope/vim-surround'

" 代码格式化
Plug 'dense-analysis/ale'

call plug#end()

" ============================================================================
" 插件配置
" ============================================================================

" NERDTree
nnoremap <leader>n :NERDTreeToggle<CR>
let NERDTreeShowHidden=1

" fzf
nnoremap <leader>f :Files<CR>
nnoremap <leader>b :Buffers<CR>
nnoremap <leader>r :Rg<CR>

" Gruvbox 主题
colorscheme gruvbox

" Airline
let g:airline_theme='gruvbox'
let g:airline_powerline_fonts = 1
let g:airline#extensions#tabline#enabled = 1

" ALE
let g:ale_linters = {
\   'java': ['javac'],
\}
let g:ale_fixers = {
\   '*': ['remove_trailing_lines', 'trim_whitespace'],
\}
```

### 安装插件步骤

1. 安装 vim-plug（见上文）
2. 将插件配置添加到 `.vimrc`
3. 打开 Vim
4. 执行 `:PlugInstall`
5. 等待安装完成
6. 重启 Vim

---

## 故障排除

### 问题 1：Vim 模式失效

**症状：** 打开文件后 Vim 操作不正常，基本功能缺失

**解决方法：**
1. 检查 `.vimrc` 是否存在且不为空
   ```bash
   cat ~/.vimrc
   ```
2. 确保 `set nocompatible` 在文件开头
3. 重新加载配置：
   ```vim
   :source ~/.vimrc
   ```

### 问题 2：语法高亮不工作

**解决方法：**
```vim
:syntax on
:filetype detect
```

或在 `.vimrc` 中确保有：
```vim
syntax on
filetype plugin indent on
```

### 问题 3：系统剪贴板不工作

**检查 Vim 是否支持剪贴板：**
```bash
vim --version | grep clipboard
```

如果显示 `-clipboard`，需要安装支持剪贴板的版本：
```bash
# Ubuntu
sudo apt install vim-gtk3

# macOS (使用 Homebrew)
brew install vim
```

### 问题 4：撤销历史无法保存

**解决方法：**

确保目录存在：
```bash
mkdir -p ~/.vim/undodir
mkdir -p ~/.vim/backup
mkdir -p ~/.vim/swap
```

### 问题 5：相对行号不显示

**解决方法：**
```vim
:set number
:set relativenumber
```

或在 `.vimrc` 中添加：
```vim
set number
set relativenumber
```

### 问题 6：配色显示异常

**解决方法：**

检查终端是否支持真彩色：
```bash
echo $TERM
```

如果不支持，在 `.vimrc` 中注释掉：
```vim
" if has('termguicolors')
"     set termguicolors
" endif
```

### 问题 7：Java 文件缩进不正确

**解决方法：**

手动设置 Java 文件的缩进：
```vim
:set tabstop=4 shiftwidth=4 expandtab
```

或确保 `.vimrc` 中有：
```vim
autocmd FileType java setlocal tabstop=4 shiftwidth=4 softtabstop=4 expandtab
```

### 问题 8：插件安装失败

**解决方法：**

1. 确保网络连接正常
2. 检查 vim-plug 是否正确安装
3. 尝试手动安装：
   ```bash
   cd ~/.vim/plugged
   git clone [插件仓库 URL]
   ```
4. 更新插件管理器：
   ```vim
   :PlugUpgrade
   ```

### 问题 9：快捷键不工作

**解决方法：**

1. 检查 Leader 键是否正确设置：
   ```vim
   :echo mapleader
   ```
2. 查看键位映射：
   ```vim
   :map <leader>w
   ```
3. 确保没有键位冲突

### 问题 10：性能问题（打开大文件卡顿）

**解决方法：**

对于大文件，临时禁用某些功能：
```vim
:syntax off
:set norelativenumber
:set nowrap
:set nofoldenable
```

或在 `.vimrc` 中添加自动检测：
```vim
autocmd BufReadPre * if getfsize(expand("%")) > 10000000 | syntax off | endif
```

---

## 学习资源

### 在线教程

1. **Vim 内置教程**
   ```bash
   vimtutor
   ```
   这是最好的入门教程，大约需要 30 分钟

2. **Vim Adventures** - 游戏化学习 Vim
   - 网址：https://vim-adventures.com/

3. **OpenVim** - 交互式教程
   - 网址：https://www.openvim.com/

### 推荐书籍

- **《Practical Vim》** by Drew Neil
- **《Learning Vi and Vim Editors》** by Arnold Robbins

### Cheat Sheet

- Vim Cheat Sheet: https://vim.rtorr.com/
- Graphical Cheat Sheet: https://www.viemu.com/vi-vim-cheat-sheet.gif

### 视频教程

- YouTube 搜索 "Vim Tutorial"
- ThePrimeagen 的 Vim 系列视频

---

## 常见问题 FAQ

**Q: Vim 和 Neovim 有什么区别？**

A: Neovim 是 Vim 的现代化分支，主要改进包括：
- 更好的插件架构
- 异步支持
- 内置终端
- 更现代的默认设置
- Lua 脚本支持

**Q: 我需要学习所有的 Vim 命令吗？**

A: 不需要。掌握基本的移动、编辑、搜索命令就足够日常使用了。高级功能可以根据需要逐步学习。

**Q: 如何快速提高 Vim 使用效率？**

A:
1. 完成 `vimtutor` 教程
2. 强迫自己不使用方向键和鼠标
3. 每天学习一个新命令
4. 实际项目中持续使用
5. 观看他人的 Vim 使用视频

**Q: 我应该使用 Vim 还是 VS Code？**

A: 这取决于你的需求：
- **Vim**: 轻量、快速、强大的编辑能力、远程开发友好
- **VS Code**: 图形界面、丰富的扩展、调试工具、易于上手

你也可以在 VS Code 中使用 Vim 插件，获得两者的优点。

**Q: 如何退出 Vim？**

A:
- `:q` - 退出
- `:q!` - 强制退出（不保存）
- `:wq` - 保存并退出
- `ZZ` - 保存并退出（普通模式）

---

## 贡献和反馈

如果你有任何建议或发现问题，欢迎：
- 提交 Issue
- 发起 Pull Request
- 分享你的配置改进

---

## 许可证

本配置文件采用 MIT 许可证，你可以自由使用、修改和分发。

---

## 更新日志

### 2025-11-05
- 初始版本发布
- 添加基础 Vim 配置
- 添加自定义键位映射
- 添加 Java 文件支持
- 创建完整文档

---

**祝你使用愉快！Happy Vimming! 🚀**
